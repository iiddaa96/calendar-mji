window.addEventListener("DOMContentLoaded", main);

function main() {
  loadFromLocalStorage();
  startTodos();
  initWelcome();
  initCalendar();
  // initCalendar();
  // console.log('Hi students!');
  // Init today view
  // Init todos list
  // Init calendar
}
