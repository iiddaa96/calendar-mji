window.addEventListener("DOMContentLoaded", main);

function main() {
  startTodos();
  initWelcome();
  initCalendar();
  // console.log('Hi students!');
  // Init today view
  // Init todos list
  // Init calendar
}
